/**
 * Mission partners are user-managed content.
 * Keep the baseline empty until real partner records are created in admin.
 */

export const partnersSeed: {
  name: string;
  logoUrl: string;
  websiteUrl: string;
  displayOrder: number;
  isPublished: boolean;
}[] = [];
